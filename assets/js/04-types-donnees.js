/* --------------------------- TYPES DE DONNEES PRIMITIFS --------------------------- */

// STRING (chaine de caractère)
const message = 'Je suis du texte';

// NUMBER (Tous les types d'écriture numériques sont possibles : positif, negatif, decimal)
const number = 89; // NUMBER
const negatif = -10; // NUMBER
let decimal = 56.8 // NUMBER

// BOOLEAN (booléens)
// Variable de type booleen, n'accepte que 2 valeurs TRUE ou FALSE
const boolean = true
const boolean2 = false

const rien = null

let test; //undefined

decimal = null

console.log(typeof message);
console.log(typeof number);
console.log(typeof negatif);
console.log(typeof decimal);
console.log(typeof boolean);
console.log(typeof boolean2);
console.log(typeof test);
console.log(typeof rien);
